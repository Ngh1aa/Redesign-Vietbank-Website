node_modules/
dist/
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/

# Upstream skills_UIUX clone — external dependency, not vendored source.
# The installed subset lives in `.claude/skills/`. Re-clone with:
#   git clone https://github.com/Ngh1aa/skills_UIUX
/skills_UIUX/
